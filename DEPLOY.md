# 🌐 Деплой Zingram БЕЗ IP и ngrok

## Вариант 1: Railway (РЕКОМЕНДУЕТСЯ) ⭐

**Самый простой способ! Бесплатно, постоянная ссылка!**

### Шаги:

1. **Зарегистрируйся на Railway:**
   - Зайди на https://railway.app
   - Войди через GitHub

2. **Создай новый проект:**
   - Нажми "New Project"
   - Выбери "Deploy from GitHub repo"
   - Если нет GitHub репозитория, создай:

3. **Создай GitHub репозиторий:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ТвойUsername/zingram.git
   git push -u origin main
   ```

4. **Подключи репозиторий к Railway:**
   - Выбери свой репозиторий
   - Railway автоматически задеплоит!

5. **Получи ссылку:**
   - Railway выдаст URL типа: `https://zingram-production.up.railway.app`
   - Отправь эту ссылку другу!

**Готово! Теперь вы оба можете зайти по этой ссылке из любой точки мира!**

---

## Вариант 2: Render (Тоже бесплатно)

1. Зайди на https://render.com
2. Войди через GitHub
3. Нажми "New +" → "Web Service"
4. Подключи свой GitHub репозиторий
5. Настройки:
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
6. Нажми "Create Web Service"
7. Получишь URL типа: `https://zingram.onrender.com`

---

## Вариант 3: Glitch (Самый быстрый)

1. Зайди на https://glitch.com
2. Нажми "New Project" → "Import from GitHub"
3. Вставь ссылку на свой репозиторий
4. Glitch автоматически запустит проект
5. Получишь URL типа: `https://zingram.glitch.me`

**Минус:** Glitch засыпает после 5 минут неактивности

---

## Вариант 4: Heroku (Классика)

1. Зарегистрируйся на https://heroku.com
2. Установи Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli
3. В терминале:
   ```bash
   heroku login
   heroku create zingram-твоеимя
   git push heroku main
   ```
4. Получишь URL: `https://zingram-твоеимя.herokuapp.com`

---

## Вариант 5: Vercel (Для фронтенда)

**Внимание:** Vercel не поддерживает WebSocket напрямую, нужен отдельный бэкенд!

1. Задеплой бэкенд на Railway/Render
2. Фронтенд на Vercel:
   ```bash
   npm i -g vercel
   vercel
   ```

---

## 🎯 Рекомендация:

**Используй Railway!** Это самый простой и надежный вариант.

### Почему Railway?
✅ Бесплатно (500 часов в месяц)
✅ Автоматический деплой при push в GitHub
✅ Поддержка WebSocket
✅ Постоянная ссылка
✅ Не засыпает
✅ Простая настройка

---

## После деплоя:

1. Получи свою ссылку (например: `https://zingram.up.railway.app`)
2. Отправь другу
3. Вы оба регистрируетесь
4. Начинаете общаться!

**Все работает автоматически! Никаких IP, никаких настроек!** 🔥

---

## Быстрый старт с Railway (без GitHub):

Если не хочешь возиться с GitHub:

1. Зайди на https://railway.app
2. Нажми "New Project" → "Empty Project"
3. Нажми "Deploy from GitHub" → "Configure GitHub App"
4. Или используй Railway CLI:
   ```bash
   npm i -g @railway/cli
   railway login
   railway init
   railway up
   ```

Готово! Railway выдаст ссылку!

---

## Проблемы?

### Ошибка деплоя:
- Проверь что `package.json` правильный
- Убедись что `node_modules` в `.gitignore`

### WebSocket не работает:
- Убедись что используешь HTTPS (не HTTP)
- Railway/Render автоматически дают HTTPS

### Друг не может подключиться:
- Проверь что отправил правильную ссылку
- Убедись что сервер запущен (зайди сам)

---

**Теперь у тебя постоянная ссылка на мессенджер! Делись с друзьями!** 🚀
