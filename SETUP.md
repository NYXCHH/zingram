# 🚀 Установка и запуск Zingram

## Требования

- Node.js (версия 16 или выше)
- npm (устанавливается вместе с Node.js)

## Установка Node.js

### Windows:
1. Скачай с https://nodejs.org/
2. Установи LTS версию
3. Перезагрузи компьютер

## Запуск сервера

### 1. Установи зависимости:
```bash
npm install
```

### 2. Запусти сервер:
```bash
npm start
```

Сервер запустится на http://localhost:3000

## Тестирование с другом

### Вариант 1: Локальная сеть (WiFi)
1. Узнай свой IP адрес:
   - Windows: `ipconfig` (ищи IPv4 Address)
   - Пример: `192.168.1.100`

2. Запусти сервер:
   ```bash
   npm start
   ```

3. Скажи другу открыть в браузере:
   ```
   http://ВАШ_IP:3000
   ```
   Например: `http://192.168.1.100:3000`

4. Вы оба должны быть в одной WiFi сети!

### Вариант 2: Интернет (через ngrok)

1. Установи ngrok: https://ngrok.com/download

2. Запусти сервер:
   ```bash
   npm start
   ```

3. В другом терминале запусти ngrok:
   ```bash
   ngrok http 3000
   ```

4. Ngrok выдаст публичный URL типа:
   ```
   https://abc123.ngrok.io
   ```

5. Отправь этот URL другу - он сможет зайти из любой точки мира!

6. **ВАЖНО:** Обнови `API_URL` в файле `public/client.js`:
   ```javascript
   const API_URL = 'https://abc123.ngrok.io';
   ```

### Вариант 3: Хостинг (для постоянного доступа)

Можно задеплоить на:
- **Heroku** (бесплатно)
- **Railway** (бесплатно)
- **Render** (бесплатно)
- **Vercel** (для фронтенда)

## Использование

1. Открой http://localhost:3000
2. Зарегистрируйся (придумай пароль минимум 6 символов)
3. Найди друга по @username или номеру телефона
4. Начни общаться!

## Функции

✅ Регистрация и вход с паролем
✅ Реальная синхронизация сообщений
✅ Статусы онлайн/офлайн
✅ Голосовые/видео звонки через WebRTC
✅ Поиск пользователей
✅ Отправка файлов
✅ Голосовые сообщения
✅ Игры во время звонка
✅ 10 языков интерфейса

## Проблемы?

### Порт 3000 занят:
Измени порт в `server.js`:
```javascript
const PORT = 3001; // или любой другой
```

### Звонки не работают:
- Разреши доступ к микрофону в браузере
- Используй HTTPS (через ngrok) для WebRTC

### Друг не может подключиться:
- Проверь что вы в одной WiFi сети
- Отключи файрвол/антивирус временно
- Используй ngrok для доступа через интернет

## Разработка

Для автоперезагрузки при изменениях:
```bash
npm run dev
```

---

**Готово! Теперь у тебя реальный рабочий мессенджер! 🔥**
